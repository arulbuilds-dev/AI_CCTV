AI_CCTV
========

Project: AI CCTV with Python APIs and dashboard.

Quick start
-----------

1. Create a Python virtual environment and install dependencies:

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

2. Run tests or examples (inspect `src/` and top-level scripts):

```bash
python gpu_test.py
python main.py
```

Repository
----------

This repo contains models and large files under `data/`, `models/`, and `outputs/` which are ignored by `.gitignore`.

Next steps I can do for you:
- Initialize a local git repo and make the initial commit (I can do this now).
- Create a GitHub repository and push the code (I will need your permission/credentials or `gh`/token).
- Audit the codebase and produce a short module summary and dependency list.
